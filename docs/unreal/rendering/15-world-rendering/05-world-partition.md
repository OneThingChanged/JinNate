# 월드 파티션

UE5의 World Partition은 대규모 오픈 월드를 효율적으로 관리하고 스트리밍하는 시스템입니다.

---

## 아키텍처 개요

```
┌─────────────────────────────────────────────────────────────────┐
│                  World Partition 아키텍처                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    UWorld (Partitioned)                  │   │
│  │                                                          │   │
│  │  ┌───────────────────────────────────────────────────┐  │   │
│  │  │              World Partition Subsystem             │  │   │
│  │  │                                                    │  │   │
│  │  │  ┌─────────────────────────────────────────────┐  │  │   │
│  │  │  │            Runtime Hash Grid                 │  │  │   │
│  │  │  │  ┌────┬────┬────┬────┬────┬────┬────┬────┐  │  │  │   │
│  │  │  │  │    │    │    │    │    │    │    │    │  │  │  │   │
│  │  │  │  ├────┼────┼────┼────┼────┼────┼────┼────┤  │  │  │   │
│  │  │  │  │    │ L  │ L  │ L  │    │    │    │    │  │  │  │   │
│  │  │  │  ├────┼────┼────┼────┼────┼────┼────┼────┤  │  │  │   │
│  │  │  │  │    │ L  │ P  │ L  │    │    │    │    │  │  │  │   │
│  │  │  │  ├────┼────┼────┼────┼────┼────┼────┼────┤  │  │  │   │
│  │  │  │  │    │ L  │ L  │ L  │    │    │    │    │  │  │  │   │
│  │  │  │  └────┴────┴────┴────┴────┴────┴────┴────┘  │  │  │   │
│  │  │  │  P = Player, L = Loaded Cell                │  │  │   │
│  │  │  └─────────────────────────────────────────────┘  │  │   │
│  │  └───────────────────────────────────────────────────┘  │   │
│  │                                                          │   │
│  │  특징:                                                    │   │
│  │  • One File Per Actor (OFPA)                            │   │
│  │  • 자동 셀 기반 스트리밍                                  │   │
│  │  • Data Layers 지원                                      │   │
│  │  • 협업 친화적                                            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## One File Per Actor (OFPA)

### 파일 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                    OFPA 파일 구조                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  기존 방식 (단일 .umap)              OFPA 방식                   │
│  ┌─────────────────────┐            ┌─────────────────────┐    │
│  │ MyLevel.umap        │            │ MyLevel/            │    │
│  │                     │            │ ├── __ExternalActors│    │
│  │ • Actor A           │            │ │   ├── Actor_A.uasset  │
│  │ • Actor B           │   ──────▶  │ │   ├── Actor_B.uasset  │
│  │ • Actor C           │            │ │   ├── Actor_C.uasset  │
│  │ • Actor D           │            │ │   └── Actor_D.uasset  │
│  │ • ...               │            │ └── MyLevel.umap   │    │
│  │                     │            │     (최소 데이터)   │    │
│  └─────────────────────┘            └─────────────────────┘    │
│                                                                 │
│  장점:                                                          │
│  • 협업 시 충돌 최소화                                          │
│  • 개별 액터 버전 관리                                          │
│  • 부분적 로드/언로드 가능                                      │
│  • 빠른 저장/로드                                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Actor 저장

```cpp
// 외부 액터 설정
class AActor
{
    // 외부 저장 여부
    UPROPERTY()
    EActorPackagingMode PackageMode;
    // Internal - 레벨 파일에 저장
    // External - 개별 파일로 저장

    // 외부 패키지 경로
    FString GetExternalPackageName() const
    {
        return FString::Printf(TEXT("%s/__ExternalActors/%s"),
            *GetLevel()->GetPackage()->GetName(),
            *GetActorGuid().ToString());
    }
};

// 액터 패키지 생성
UPackage* CreateActorPackage(AActor* Actor)
{
    FString PackageName = Actor->GetExternalPackageName();
    UPackage* Package = CreatePackage(*PackageName);

    // 액터를 패키지로 이동
    Actor->Rename(nullptr, Package);

    return Package;
}
```

---

## Runtime Grid

### 그리드 구조

```cpp
// 런타임 그리드 설정
class URuntimeHashSetPartitioned : public UObject
{
    // 그리드 설정
    UPROPERTY(EditAnywhere)
    FName Name;

    // 셀 크기 (cm)
    UPROPERTY(EditAnywhere)
    int32 CellSize = 12800;  // 128m

    // 로딩 범위
    UPROPERTY(EditAnywhere)
    int32 LoadingRange = 25600;  // 256m

    // 블록 사이즈 (빌드용)
    UPROPERTY(EditAnywhere)
    int32 BlockSize = 1;

    // 우선순위
    UPROPERTY(EditAnywhere)
    int32 Priority = 0;

    // Debug 표시
    UPROPERTY(EditAnywhere)
    bool bDebugDraw = false;
};
```

### 셀 로딩 로직

```
┌─────────────────────────────────────────────────────────────────┐
│                    셀 로딩 알고리즘                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                      Grid View                           │   │
│  │                                                          │   │
│  │     Loading Range                                        │   │
│  │     ┌───────────────────────────────┐                   │   │
│  │     │   ┌───────────────────┐       │                   │   │
│  │     │   │   Keep Loaded     │       │                   │   │
│  │     │   │   ┌───────────┐   │       │                   │   │
│  │     │   │   │           │   │       │                   │   │
│  │     │   │   │  Player   │   │       │                   │   │
│  │     │   │   │           │   │       │                   │   │
│  │     │   │   └───────────┘   │       │                   │   │
│  │     │   │                   │       │                   │   │
│  │     │   └───────────────────┘       │                   │   │
│  │     │                               │                   │   │
│  │     └───────────────────────────────┘                   │   │
│  │                                                          │   │
│  │  로딩 로직:                                               │   │
│  │  1. 플레이어 위치의 셀 확인                               │   │
│  │  2. Loading Range 내 셀 로드                             │   │
│  │  3. Keep Loaded Range 외 셀 언로드                       │   │
│  │  4. 히스테리시스로 빈번한 로드/언로드 방지                 │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  플레이어 이동 시:                                              │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  이전 위치          새 위치                              │   │
│  │     │                  │                                 │   │
│  │     ▼                  ▼                                 │   │
│  │  [A][B][C]         [B][C][D]                             │   │
│  │   ↑                     ↑                                │   │
│  │   │                     │                                │   │
│  │   Unload A             Load D                            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Data Layers

### 레이어 시스템

```cpp
// Data Layer 에셋
class UDataLayerAsset : public UObject
{
    // 레이어 타입
    EDataLayerType DataLayerType;
    // Runtime    - 런타임에 토글 가능
    // Editor     - 에디터에서만 사용

    // 초기 상태
    EDataLayerRuntimeState InitialRuntimeState;
    // Unloaded
    // Loaded
    // Activated

    // 부모 레이어
    UDataLayerAsset* ParentDataLayer;

    // Debug 색상
    FColor DebugColor;
};

// Data Layer 인스턴스
class UDataLayerInstance : public UObject
{
    // 에셋 참조
    TSoftObjectPtr<UDataLayerAsset> DataLayerAsset;

    // 런타임 상태
    EDataLayerRuntimeState RuntimeState;

    // 소속 액터들
    TArray<TWeakObjectPtr<AActor>> Actors;
};
```

### 사용 예시

```
┌─────────────────────────────────────────────────────────────────┐
│                   Data Layer 활용 예시                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  게임 진행 기반 레이어:                                          │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │  ┌──────────────┐     ┌──────────────┐                  │   │
│  │  │ Base Layer   │     │ Quest Layer  │                  │   │
│  │  │              │     │              │                  │   │
│  │  │ • 기본 지형  │     │ • NPC        │                  │   │
│  │  │ • 건물       │     │ • 퀘스트 오브젝트│                │   │
│  │  │ • 도로       │     │ • 이벤트 트리거 │                │   │
│  │  │              │     │              │                  │   │
│  │  │ [항상 로드]  │     │ [조건부 로드]│                   │   │
│  │  └──────────────┘     └──────────────┘                  │   │
│  │                                                          │   │
│  │  ┌──────────────┐     ┌──────────────┐                  │   │
│  │  │ Day Layer    │     │ Night Layer  │                  │   │
│  │  │              │     │              │                  │   │
│  │  │ • 낮 조명    │     │ • 밤 조명    │                  │   │
│  │  │ • 낮 NPC     │     │ • 밤 NPC     │                  │   │
│  │  │              │     │              │                  │   │
│  │  │ [시간 기반]  │     │ [시간 기반]  │                  │   │
│  │  └──────────────┘     └──────────────┘                  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  코드로 레이어 제어:                                             │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  // 레이어 상태 변경                                     │   │
│  │  UDataLayerSubsystem* DataLayerSubsystem =              │   │
│  │      GetWorld()->GetSubsystem<UDataLayerSubsystem>();   │   │
│  │                                                          │   │
│  │  DataLayerSubsystem->SetDataLayerRuntimeState(          │   │
│  │      QuestLayer,                                        │   │
│  │      EDataLayerRuntimeState::Activated                  │   │
│  │  );                                                      │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Level Instance

### Packed Level Instance

```cpp
// Level Instance Actor
class ALevelInstance : public AActor
{
    // 레벨 에셋
    UPROPERTY(EditAnywhere)
    TSoftObjectPtr<UWorld> WorldAsset;

    // 인스턴스 설정
    UPROPERTY(EditAnywhere)
    ELevelInstanceRuntimeBehavior RuntimeBehavior;
    // None                 - 런타임에 무시
    // Embedded_InPackage   - 패키지에 포함
    // Partitioned          - World Partition 통합

    // 에디터에서 편집 가능
    UPROPERTY(EditAnywhere)
    bool bCanBeEdited;
};

// Packed Level Actor (인스턴싱 최적화)
class APackedLevelActor : public ALevelInstance
{
    // 패킹된 데이터
    UPROPERTY()
    UPackedLevelActorBuilder* PackedData;

    // 인스턴스별 변형 가능
    bool bAllowPerInstanceEdits;
};
```

### 사용 사례

```
┌─────────────────────────────────────────────────────────────────┐
│                   Level Instance 사용 사례                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  반복 구조물:                                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │  Building_01.umap (소스 레벨)                            │   │
│  │  ┌─────────────┐                                         │   │
│  │  │  ▓▓▓▓▓▓▓▓  │                                         │   │
│  │  │  ▓      ▓  │                                         │   │
│  │  │  ▓      ▓  │                                         │   │
│  │  │  ▓▓▓▓▓▓▓▓  │                                         │   │
│  │  └─────────────┘                                         │   │
│  │        │                                                 │   │
│  │        │ Level Instance로 배치                           │   │
│  │        ▼                                                 │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │   │
│  │  │  Instance 1 │  │  Instance 2 │  │  Instance 3 │      │   │
│  │  │  Transform A│  │  Transform B│  │  Transform C│      │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘      │   │
│  │                                                          │   │
│  │  장점:                                                    │   │
│  │  • 데이터 공유로 메모리 절약                              │   │
│  │  • 소스 수정 시 모든 인스턴스 업데이트                    │   │
│  │  • 개별 인스턴스 커스터마이징 가능                        │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Streaming Source

### 스트리밍 소스 설정

```cpp
// 스트리밍 소스 컴포넌트
class UWorldPartitionStreamingSourceComponent : public UActorComponent
{
    // 스트리밍 소스 활성화
    UPROPERTY(EditAnywhere)
    bool bStreamingSourceEnabled = true;

    // 타겟 그리드
    UPROPERTY(EditAnywhere)
    FName TargetGridName;

    // 로딩 우선순위
    UPROPERTY(EditAnywhere)
    EStreamingSourcePriority Priority;

    // 디버그 표시
    UPROPERTY(EditAnywhere)
    bool bShowDebug = false;

    // 로딩 범위 오버라이드
    UPROPERTY(EditAnywhere)
    bool bOverrideLoadingRange = false;

    UPROPERTY(EditAnywhere)
    float LoadingRangeOverride = 0.0f;
};
```

### 다중 스트리밍 소스

```
┌─────────────────────────────────────────────────────────────────┐
│                   다중 Streaming Source                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    World Grid                            │   │
│  │                                                          │   │
│  │     ┌─────────────────┐     ┌─────────────────┐         │   │
│  │     │   Player 1      │     │   Player 2      │         │   │
│  │     │   [Source]      │     │   [Source]      │         │   │
│  │     │     ○           │     │        ○        │         │   │
│  │     │   ╱   ╲         │     │      ╱   ╲      │         │   │
│  │     │  ╱     ╲        │     │     ╱     ╲     │         │   │
│  │     │ ╱       ╲       │     │    ╱       ╲    │         │   │
│  │     │[Loading Range]  │     │  [Loading Range]│         │   │
│  │     └─────────────────┘     └─────────────────┘         │   │
│  │                                                          │   │
│  │     ┌─────────────────┐                                 │   │
│  │     │   AI Director   │                                 │   │
│  │     │   [Source]      │  ◀── 커스텀 스트리밍 소스       │   │
│  │     │     ○           │     (AI 스폰 영역 미리 로드)   │   │
│  │     │   ╱   ╲         │                                 │   │
│  │     │ [Custom Range]  │                                 │   │
│  │     └─────────────────┘                                 │   │
│  │                                                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  멀티플레이어:                                                   │
│  • 각 플레이어가 독립적 스트리밍 소스                           │
│  • 합집합 영역이 로드됨                                         │
│  • 서버에서 관리 (Dedicated Server)                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 에디터 통합

### 레벨 에디터

```
┌─────────────────────────────────────────────────────────────────┐
│                World Partition 에디터 기능                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 영역 기반 로딩                                               │
│     ┌─────────────────────────────────────────────────────┐    │
│     │ • 에디터에서 필요한 영역만 로드                        │    │
│     │ • Region Selection Tool                             │    │
│     │ • 메모리 효율적 대규모 맵 편집                        │    │
│     └─────────────────────────────────────────────────────┘    │
│                                                                 │
│  2. World Partition 에디터                                       │
│     ┌─────────────────────────────────────────────────────┐    │
│     │ ┌────────────────────────────────────────────────┐ │    │
│     │ │           World Partition Window               │ │    │
│     │ │  ┌────┬────┬────┬────┬────┬────┬────┬────┐   │ │    │
│     │ │  │    │    │ ██ │ ██ │    │    │    │    │   │ │    │
│     │ │  ├────┼────┼────┼────┼────┼────┼────┼────┤   │ │    │
│     │ │  │    │ ██ │ ██ │ ██ │ ██ │    │    │    │   │ │    │
│     │ │  ├────┼────┼────┼────┼────┼────┼────┼────┤   │ │    │
│     │ │  │    │ ██ │ 📍 │ ██ │ ██ │    │    │    │   │ │    │
│     │ │  └────┴────┴────┴────┴────┴────┴────┴────┘   │ │    │
│     │ │  ██ = 로드됨, 📍 = 카메라 위치               │ │    │
│     │ └────────────────────────────────────────────────┘ │    │
│     └─────────────────────────────────────────────────────┘    │
│                                                                 │
│  3. 협업 워크플로우                                              │
│     ┌─────────────────────────────────────────────────────┐    │
│     │ • 개별 액터 체크아웃                                 │    │
│     │ • 셀 단위 작업 분리                                  │    │
│     │ • 충돌 최소화                                        │    │
│     └─────────────────────────────────────────────────────┘    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 최적화 및 디버깅

### 콘솔 명령어

```cpp
// 디버깅 명령어
wp.Runtime.ToggleDrawStreamingSources      // 스트리밍 소스 시각화
wp.Runtime.ToggleDrawRuntimeHash           // 런타임 그리드 표시
wp.Editor.ShowActorDescGuidInOutliner      // 액터 GUID 표시

// 스트리밍 제어
wp.Runtime.SetWorldPartitionStreamingEnabled 1  // 스트리밍 활성화
wp.Runtime.ForceStreamingSourcePriority        // 우선순위 강제

// 통계
stat worldpartition                        // World Partition 통계
```

### 성능 모니터링

```
┌─────────────────────────────────────────────────────────────────┐
│                  World Partition 성능 지표                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  stat worldpartition 출력:                                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                          │   │
│  │  Loaded Cells:           24                              │   │
│  │  Pending Load:           3                               │   │
│  │  Pending Unload:         1                               │   │
│  │  Loaded Actors:          1,523                           │   │
│  │  Streaming Sources:      2                               │   │
│  │                                                          │   │
│  │  Memory Usage:                                           │   │
│  │    Cell Data:            45.2 MB                         │   │
│  │    Actor Data:           128.5 MB                        │   │
│  │    HLOD Data:            32.1 MB                         │   │
│  │                                                          │   │
│  │  Streaming Performance:                                  │   │
│  │    Load Time (avg):      12.3 ms                         │   │
│  │    Unload Time (avg):    5.2 ms                          │   │
│  │                                                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  최적화 체크리스트:                                              │
│  □ 적절한 셀 크기 (너무 작으면 오버헤드)                        │
│  □ 로딩 범위 튜닝 (너무 크면 메모리 부족)                       │
│  □ Data Layer 활용 (불필요한 로드 방지)                        │
│  □ HLOD 설정 최적화                                            │
│  □ 스트리밍 소스 최소화                                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 마이그레이션

### 기존 레벨 변환

```cpp
// World Partition 활성화 단계
// 1. 프로젝트 설정
// Project Settings → Engine → World Partition → Enable World Partition

// 2. 레벨 변환
// Level → World Partition → Convert Level

// 변환 과정
void ConvertToWorldPartition(UWorld* World)
{
    // 1. OFPA 활성화
    World->SetPackageFlags(PKG_ContainsExternalActors);

    // 2. 기존 액터들을 외부 파일로 분리
    for (AActor* Actor : World->GetActors())
    {
        if (ShouldExternalize(Actor))
        {
            Actor->SetPackageMode(EActorPackagingMode::External);
        }
    }

    // 3. 런타임 그리드 생성
    UWorldPartitionRuntimeHash* RuntimeHash = CreateRuntimeHash(World);

    // 4. 셀 할당
    AssignActorsToCells(World, RuntimeHash);
}
```

---

## 요약

World Partition은 UE5의 핵심 기능으로, 대규모 오픈 월드 개발을 위한 강력한 도구입니다:

| 기능 | 효과 |
|------|------|
| **OFPA** | 협업 효율 향상 |
| **Runtime Grid** | 자동 스트리밍 |
| **Data Layers** | 조건부 콘텐츠 |
| **Level Instance** | 재사용성 |
| **HLOD 통합** | 원거리 최적화 |

---

## 참고 자료

- [Unreal Engine World Partition 문서](https://docs.unrealengine.com/world-partition/)
- [Building Open Worlds (GDC 2022)](https://www.youtube.com/watch?v=...)
- [World Partition Deep Dive](https://dev.epicgames.com/documentation/)

# 04. 파티클 렌더링

Niagara 렌더러 타입, Sprite/Mesh/Ribbon 렌더링, 정렬 시스템을 분석합니다.

---

## 렌더러 개요

### 렌더러 타입

```
┌─────────────────────────────────────────────────────────────────┐
│                    Niagara 렌더러 타입                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Sprite Renderer:                                               │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  - 빌보드 (카메라 향함)                                 │   │
│  │  - 가장 일반적                                          │   │
│  │  - 불, 연기, 스파크 등                                  │   │
│  │  ┌───┐                                                  │   │
│  │  │░░░│ ← 항상 카메라를 향함                             │   │
│  │  └───┘                                                  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  Mesh Renderer:                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  - 3D 메시 인스턴싱                                     │   │
│  │  - 돌, 파편, 잎 등                                      │   │
│  │  ┌───┐  ┌───┐  ┌───┐                                   │   │
│  │  │ ◇ │  │ ◇ │  │ ◇ │ ← 각각 회전/스케일 가능          │   │
│  │  └───┘  └───┘  └───┘                                   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  Ribbon Renderer:                                               │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  - 연결된 스트립                                        │   │
│  │  - 트레일, 레이저, 번개                                 │   │
│  │  ╭─────────────────────╮                               │   │
│  │  │░░░░░░░░░░░░░░░░░░░░│ ← 파티클 연결                  │   │
│  │  ╰─────────────────────╯                               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  Light Renderer:                                                │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  - 다이나믹 라이트                                      │   │
│  │  - 불꽃, 섬광 효과                                      │   │
│  │      ✦ ← 광원 생성                                      │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  기타:                                                          │
│  - Component Renderer (Actor/Component)                        │
│  - Decal Renderer                                              │
│  - Volume Renderer                                             │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Sprite Renderer

### Sprite 기본 구조

```cpp
// Sprite 렌더러 프로퍼티
class UNiagaraSpriteRendererProperties : public UNiagaraRendererProperties
{
    // 머티리얼
    UPROPERTY()
    UMaterialInterface* Material;

    // 정렬 모드
    UPROPERTY()
    ENiagaraSpriteAlignment Alignment;

    UPROPERTY()
    ENiagaraSpriteFacingMode FacingMode;

    // 소스 모드 (커스텀 정렬 축)
    UPROPERTY()
    ENiagaraSortMode SortMode;

    // Sub UV (스프라이트 시트)
    UPROPERTY()
    int32 SubImageIndexX;
    UPROPERTY()
    int32 SubImageIndexY;

    // 컷아웃 (마스킹)
    UPROPERTY()
    bool bEnableCutout;

    // 바인딩 (파티클 속성 → 렌더러)
    UPROPERTY()
    FNiagaraVariableAttributeBinding PositionBinding;
    UPROPERTY()
    FNiagaraVariableAttributeBinding ColorBinding;
    UPROPERTY()
    FNiagaraVariableAttributeBinding SpriteSizeBinding;
    UPROPERTY()
    FNiagaraVariableAttributeBinding SpriteRotationBinding;
};
```

### Sprite 정렬 모드

```
┌─────────────────────────────────────────────────────────────────┐
│                    Sprite 정렬/페이싱 모드                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Alignment:                                                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Unaligned       : 회전 없음 (기본)                     │   │
│  │  VelocityAligned : 속도 방향 정렬                       │   │
│  │  CustomAlignment : 커스텀 축                            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  Facing Mode:                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  FaceCamera         : 카메라 위치 향함 (3D 빌보드)      │   │
│  │  FaceCameraPlane    : 카메라 평면 향함 (2D 느낌)        │   │
│  │  FaceCameraPosition : 카메라 정확히 향함                │   │
│  │  FaceCameraDistance : 거리에 따른 혼합                  │   │
│  │  CustomFacing       : 커스텀 방향                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  시각화:                                                        │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                         │   │
│  │    FaceCamera:         FaceCameraPlane:                 │   │
│  │        ┌──┐                ┌──┐                         │   │
│  │       ╱    ╲              │    │                        │   │
│  │      │  👁  │ ← Camera   │    │ ← Camera               │   │
│  │       ╲    ╱              │    │                        │   │
│  │        └──┘                └──┘                         │   │
│  │    (각 스프라이트     (모든 스프라이트                  │   │
│  │     개별 회전)         동일 회전)                       │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Sprite 버텍스 생성

```hlsl
// Sprite 버텍스 셰이더 (개념적)

struct FParticleData
{
    float3 Position;
    float2 Size;
    float Rotation;
    float4 Color;
    float2 SubImageIndex;
};

StructuredBuffer<FParticleData> Particles;

void SpriteVS(
    uint VertexID : SV_VertexID,
    uint InstanceID : SV_InstanceID,
    out float4 OutPosition : SV_Position,
    out float2 OutUV : TEXCOORD0,
    out float4 OutColor : COLOR)
{
    FParticleData Particle = Particles[InstanceID];

    // 쿼드 코너 (0-3)
    float2 Corner = float2(
        (VertexID & 1) ? 0.5 : -0.5,
        (VertexID & 2) ? 0.5 : -0.5
    );

    // 회전 적용
    float CosR = cos(Particle.Rotation);
    float SinR = sin(Particle.Rotation);
    float2 RotatedCorner = float2(
        Corner.x * CosR - Corner.y * SinR,
        Corner.x * SinR + Corner.y * CosR
    );

    // 크기 적용
    float2 LocalPos = RotatedCorner * Particle.Size;

    // 빌보드 변환
    float3 Right = View.ViewRight;
    float3 Up = View.ViewUp;
    float3 WorldPos = Particle.Position + Right * LocalPos.x + Up * LocalPos.y;

    OutPosition = mul(float4(WorldPos, 1), View.ViewProjectionMatrix);
    OutUV = Corner + 0.5;  // 0-1 범위로
    OutColor = Particle.Color;
}
```

---

## Mesh Renderer

### Mesh 인스턴싱

```cpp
// Mesh 렌더러 프로퍼티
class UNiagaraMeshRendererProperties : public UNiagaraRendererProperties
{
    // 렌더링할 메시
    UPROPERTY()
    TArray<FNiagaraMeshMaterialOverride> Meshes;

    // 머티리얼 오버라이드
    UPROPERTY()
    bool bOverrideMaterials;
    UPROPERTY()
    TArray<FNiagaraMeshMaterialOverride> OverrideMaterials;

    // 정렬 모드
    UPROPERTY()
    ENiagaraSortMode SortMode;

    // 페이싱 모드
    UPROPERTY()
    ENiagaraMeshFacingMode FacingMode;

    // LOD
    UPROPERTY()
    bool bEnableMinimumLOD;
    UPROPERTY()
    int32 MinimumLOD;

    // 바인딩
    UPROPERTY()
    FNiagaraVariableAttributeBinding PositionBinding;
    UPROPERTY()
    FNiagaraVariableAttributeBinding ScaleBinding;
    UPROPERTY()
    FNiagaraVariableAttributeBinding MeshOrientationBinding;
    UPROPERTY()
    FNiagaraVariableAttributeBinding MeshIndexBinding;  // 여러 메시 중 선택
};
```

### Mesh 렌더링 파이프라인

```
┌─────────────────────────────────────────────────────────────────┐
│                    Mesh 파티클 렌더링                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. 파티클 데이터 준비                                          │
│     ┌─────────────────────────────────────────────────────┐    │
│     │  Position, Rotation, Scale, Color, MeshIndex        │    │
│     │  → GPU 버퍼에 저장                                   │    │
│     └─────────────────────────────────────────────────────┘    │
│                                                                 │
│  2. 인스턴스 데이터 생성                                        │
│     ┌─────────────────────────────────────────────────────┐    │
│     │  각 파티클 → Transform Matrix                       │    │
│     │  Position + Rotation + Scale = 4x4 Matrix           │    │
│     │  또는 Per-Instance Custom Data                       │    │
│     └─────────────────────────────────────────────────────┘    │
│                                                                 │
│  3. Instanced Draw                                              │
│     ┌─────────────────────────────────────────────────────┐    │
│     │  DrawIndexedInstanced(                              │    │
│     │      NumIndices,      // 메시 인덱스 수             │    │
│     │      NumParticles,    // 인스턴스 수                │    │
│     │      ...                                            │    │
│     │  );                                                 │    │
│     │                                                     │    │
│     │  1 드로우 콜로 모든 메시 파티클 렌더링              │    │
│     └─────────────────────────────────────────────────────┘    │
│                                                                 │
│  4. 버텍스 셰이더                                               │
│     ┌─────────────────────────────────────────────────────┐    │
│     │  - InstanceID로 파티클 데이터 읽기                  │    │
│     │  - 메시 버텍스 × 파티클 Transform                   │    │
│     │  - World/View/Projection 변환                       │    │
│     └─────────────────────────────────────────────────────┘    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Ribbon Renderer

### Ribbon 개념

```
┌─────────────────────────────────────────────────────────────────┐
│                    Ribbon Renderer                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Ribbon = 파티클을 연결한 연속적인 스트립                        │
│                                                                 │
│  파티클 순서:                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  P0 ────── P1 ────── P2 ────── P3 ────── P4            │   │
│  │  (head)                                   (tail)        │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  생성되는 지오메트리:                                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │      P0        P1        P2        P3        P4        │   │
│  │   ┌──┬──┐  ┌──┬──┐  ┌──┬──┐  ┌──┬──┐  ┌──┬──┐        │   │
│  │   │╲ │ ╱│  │╲ │ ╱│  │╲ │ ╱│  │╲ │ ╱│  │╲ │ ╱│        │   │
│  │   │ ╲│╱ │──│ ╲│╱ │──│ ╲│╱ │──│ ╲│╱ │──│ ╲│╱ │        │   │
│  │   │ ╱│╲ │  │ ╱│╲ │  │ ╱│╲ │  │ ╱│╲ │  │ ╱│╲ │        │   │
│  │   │╱ │ ╲│  │╱ │ ╲│  │╱ │ ╲│  │╱ │ ╲│  │╱ │ ╲│        │   │
│  │   └──┴──┘  └──┴──┘  └──┴──┘  └──┴──┘  └──┴──┘        │   │
│  │                                                         │   │
│  │   각 파티클 위치에서 Width만큼 확장                     │   │
│  │   이웃 파티클과 삼각형으로 연결                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  용도:                                                          │
│  - 트레일 (검 궤적, 이동 경로)                                 │
│  - 번개                                                         │
│  - 레이저 빔                                                    │
│  - 연기 꼬리                                                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### Ribbon 프로퍼티

```cpp
// Ribbon 렌더러 프로퍼티
class UNiagaraRibbonRendererProperties : public UNiagaraRendererProperties
{
    // 머티리얼
    UPROPERTY()
    UMaterialInterface* Material;

    // 페이싱 모드
    UPROPERTY()
    ENiagaraRibbonFacingMode FacingMode;

    // UV 설정
    UPROPERTY()
    ENiagaraRibbonUVMode UVMode;

    // 테셀레이션
    UPROPERTY()
    bool bEnableTessellation;
    UPROPERTY()
    int32 TessellationFactor;

    // 링크 순서
    UPROPERTY()
    ENiagaraRibbonLinkOrder LinkOrder;

    // 바인딩
    UPROPERTY()
    FNiagaraVariableAttributeBinding RibbonIdBinding;      // 리본 구분
    UPROPERTY()
    FNiagaraVariableAttributeBinding RibbonWidthBinding;   // 너비
    UPROPERTY()
    FNiagaraVariableAttributeBinding RibbonTwistBinding;   // 비틀림
    UPROPERTY()
    FNiagaraVariableAttributeBinding RibbonFacingBinding;  // 방향
};
```

### Ribbon ID 시스템

```
┌─────────────────────────────────────────────────────────────────┐
│                    Ribbon ID 시스템                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  여러 개의 독립적인 리본:                                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Ribbon ID = 0:                                         │   │
│  │  P0 ── P1 ── P2 ── P3                                  │   │
│  │                                                         │   │
│  │  Ribbon ID = 1:                                         │   │
│  │  P4 ── P5 ── P6                                        │   │
│  │                                                         │   │
│  │  Ribbon ID = 2:                                         │   │
│  │  P7 ── P8 ── P9 ── P10 ── P11                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  모듈에서 설정:                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Particle Spawn:                                        │   │
│  │    Particles.RibbonID = Emitter.SpawnCount             │   │
│  │    또는                                                 │   │
│  │    Particles.RibbonID = Particles.UniqueID             │   │
│  │                                                         │   │
│  │  같은 RibbonID를 가진 파티클만 연결됨                   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Light Renderer

### 파티클 라이트

```cpp
// Light 렌더러 프로퍼티
class UNiagaraLightRendererProperties : public UNiagaraRendererProperties
{
    // 라이트 소스 반지름
    UPROPERTY()
    float RadiusScale;

    // 세기
    UPROPERTY()
    float DefaultExponent;

    // Inverse Square 폴오프
    UPROPERTY()
    bool bUseInverseSquaredFalloff;

    // 라이트 영향 채널
    UPROPERTY()
    FLightingChannels LightingChannels;

    // 바인딩
    UPROPERTY()
    FNiagaraVariableAttributeBinding LightRadiusBinding;
    UPROPERTY()
    FNiagaraVariableAttributeBinding ColorBinding;
    UPROPERTY()
    FNiagaraVariableAttributeBinding ExponentBinding;
};
```

---

## 정렬 시스템

### 정렬 모드

```
┌─────────────────────────────────────────────────────────────────┐
│                    파티클 정렬 모드                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  None:                                                          │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  정렬 없음, 스폰 순서 유지                              │   │
│  │  가장 빠름, 불투명 파티클에 적합                        │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  View Depth:                                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  카메라에서 거리순 정렬                                 │   │
│  │  뒤에서 앞으로 (반투명용)                               │   │
│  │  ┌─────────────────────────────────────┐               │   │
│  │  │  Far ────────────────────────> Near │ → Camera      │   │
│  │  │  P3    P1    P0    P2              │               │   │
│  │  └─────────────────────────────────────┘               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  View Distance:                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  View Depth와 유사, 계산 방식 차이                      │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  Custom Ascending/Descending:                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  커스텀 속성 기준 정렬                                  │   │
│  │  예: Age, Size, Priority 등                             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  GPU 정렬:                                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  대량 파티클은 GPU에서 정렬                             │   │
│  │  Bitonic Sort, Radix Sort 등                           │   │
│  │  CPU로 데이터 복사 불필요                               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### GPU 정렬 구현

```hlsl
// Bitonic Sort (GPU 정렬 알고리즘)

RWBuffer<uint> SortKeys;    // 깊이 값 (정렬 키)
RWBuffer<uint> SortIndices; // 파티클 인덱스

// 한 단계의 비교-교환
[numthreads(256, 1, 1)]
void BitonicSortStep(
    uint DTid : SV_DispatchThreadID,
    uint GroupIdx : SV_GroupIndex,
    uint GroupId : SV_GroupID)
{
    uint i = DTid.x;

    // 비교 상대 계산
    uint j = i ^ StepDistance;

    if (j > i)
    {
        uint KeyI = SortKeys[i];
        uint KeyJ = SortKeys[j];

        // 방향에 따라 교환
        bool ShouldSwap = (i & SortDirection) ?
            (KeyI < KeyJ) : (KeyI > KeyJ);

        if (ShouldSwap)
        {
            SortKeys[i] = KeyJ;
            SortKeys[j] = KeyI;

            uint TempIdx = SortIndices[i];
            SortIndices[i] = SortIndices[j];
            SortIndices[j] = TempIdx;
        }
    }
}
```

---

## 요약

파티클 렌더링 핵심:

1. **Sprite** - 빌보드, 가장 일반적, 다양한 페이싱 모드
2. **Mesh** - 3D 메시 인스턴싱, 복잡한 형태
3. **Ribbon** - 연결된 스트립, 트레일/빔 효과
4. **Light** - 동적 광원, 불꽃 효과
5. **정렬** - 반투명 정확성, GPU 정렬 최적화

각 렌더러는 용도에 맞게 선택하고 조합합니다.

---

## 참고 자료

- [Niagara Renderers](https://docs.unrealengine.com/5.0/en-US/niagara-renderers/)
- [원문 시리즈 (중국어)](https://www.cnblogs.com/timlly/p/13512787.html)
